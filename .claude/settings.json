{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'file=$(echo $CLAUDE_TOOL_INPUT | python3 -c \"import sys,json; d=json.load(sys.stdin); print(d.get(\\\"file_path\\\",d.get(\\\"path\\\",\\\"\\\")))\" 2>/dev/null); if echo \"$file\" | grep -qE \"i18n(-pages)?\\.ts$\"; then (cd /home/user/innoacta && node scripts/translate-pages.mjs) & fi'"
          }
        ]
      }
    ]
  }
}
